# Verification layer

This is the second layer of the temporary stacked-pull-request experiment.

Expected GitHub behavior:

- the foundation pull request targets `main`;
- this pull request targets the foundation branch;
- GitHub renders both pull requests as one stack;
- both pull requests remain drafts and are not merged by this experiment.
